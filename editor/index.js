import { componentsSchema, systemsSchema, resourcesSchema } from './schema';

import en from './locales/en.json';

const locales = {
  en,
};

export {
  componentsSchema,
  systemsSchema,
  resourcesSchema,
  locales,
};
