import { componentsSchema, systemsSchema, scriptsSchema } from './schema';

import en from './locales/en.json';

const locales = {
  en,
};

export {
  componentsSchema,
  systemsSchema,
  scriptsSchema,
  locales,
};
